"""(폐기됨) Gemini Vision으로 이미지 alt 자동 생성.

Gemini 2.0 Flash 무료 티어가 quota=0이라 더 이상 호출 안 함.
파일은 향후 다른 모델로 재개할 가능성을 위해 stub만 남김.
"""
def generate_alt_from_bytes(image_bytes: bytes, mime: str = "image/jpeg") -> str:
    return ""
